* text=auto eol=lf

# Machine-written contract artifacts: collapse by default in GitHub review,
# expand on demand.
src/generated/** linguist-generated=true

# The committed plugin payload (incl. the esbuild CLI bundle) is generated by
# `npm run package:plugin`: collapse it in GitHub review and keep it out of
# language stats.
plugin/** linguist-generated=true
