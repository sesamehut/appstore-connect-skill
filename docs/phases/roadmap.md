# 项目 Roadmap

本文是 appstore-connect-skill 从设计阶段走向可分发 Skill 的阶段规划。能力边界以 [产品范围](../product/api-scope.md) 为准，分层与模块职责以 [架构总览](../architecture/overview.md) 为准。本文只回答三件事：先做什么、后做什么、每一步做到什么程度算完成。

规划基准时间：**2026-06**。阶段推进时如发现 Apple API 行为或生态变化，先复核研究笔记和官方文档，再调整本文。

## 推进原则

- **纵向切片优先**——每个阶段以"一条端到端可验证的链路"收尾，而不是按层批量铺设。请求层、分页、工作流的设计判断都需要真实能力来检验。
- **地基先行，能力跟进**——契约、认证、请求核心是所有能力的公共地基，必须先稳定；业务能力按产品首期范围分批落地。
- **Skill 形态早验证**——在首批读能力可用时就引入最小 Skill 入口，避免最后才发现任务组织或反馈形式不成立。
- **阶段文档生命周期**——每个阶段开始时在本目录建立该阶段的详细计划，完成后移入 `archive/`；本文只保留里程碑级别的状态。

## 里程碑总览

| 阶段 | 主题 | 核心产出 | 状态 |
|---|---|---|---|
| M0 | 工程基础 | 可运行的 TypeScript 工程与检查体系 | 已完成（[阶段计划](archive/m0-engineering-foundation.md)） |
| M1 | API 契约层 | 从 Apple 官方规范生成的契约与再生成流程 | 已完成（[阶段计划](archive/m1-api-contract-layer.md)） |
| M2 | 认证与请求核心 | 凭证签发与刷新、薄请求层、错误归一 | 已完成（[阶段计划](archive/m2-auth-request-core.md)） |
| M3 | 分页与首批读能力 | 分页模块与应用/版本读取切片 | 已完成（[阶段计划](archive/m3-pagination-first-reads.md)） |
| M4 | 元数据与评论能力、最小 Skill 入口 | 资源读写能力面与 Skill 雏形 | 已完成（[阶段计划](archive/m4-metadata-reviews-skill-entry.md)） |
| M5 | 报表工作流 | 销售/财务/分析报表下载链路 | 已完成（[阶段计划](archive/m5-report-workflows.md)） |
| M6 | 媒体素材工作流 | 截图与预览上传链路 | 已完成（[阶段计划](archive/m6-media-workflows.md)） |
| M7 | TestFlight 与送审准备 | 首期范围能力闭环 | 已完成（TestFlight [阶段计划](archive/m7-testflight.md) 与送审准备 [阶段计划](archive/m7-submission-release.md) 两半均已交付） |
| M8 | Skill 完备化与发布 | 任务级能力打磨、打包与使用文档 | 已完成（[阶段计划](archive/m8-skill-packaging.md)；插件以单仓库 git-subdir 形态从本仓 `plugin/` 子目录经 [marketplace](https://github.com/sesamehut/plugins-marketplace) 分发，安装命令 `/plugin install app-store-connect@sesamehut-plugins`。M8 阶段计划记录的是当时的独立 plugin 仓库方案，后调整为单仓库 git-subdir，详见 [Skill 入口与 CLI 策略](../implementation/skill-interface.md)的分发路径） |
| M9+ | 扩展与持续事项 | 域内写补全、商业化/营销/受限能力、后续集成、契约升级与生态复核 | 按需 |

依赖关系大体线性：M0 → M1 → M2 → M3 是地基链，必须按序完成；M4 之后的业务能力都依赖 M3，彼此可按价值调序，其中 M5 与 M6 互相独立，可并行或交换顺序。

## M0 工程基础

**目标**：让仓库具备开发、测试和验证代码的最小工程能力。

**范围**：TypeScript/ESM 工程骨架、测试框架选型与接入、统一的项目检查入口（类型检查、lint、测试）、CI 基本流水线。

**退出标准**：

- 仓库根目录可以一键运行全部检查，且 CI 在主分支与 PR 上执行同样的检查。
- 测试框架支持集成风格测试，外部边界可以在清晰的接缝处 mock。

本阶段的风险是过度配置：工具链以够用为准，不为尚不存在的代码预设结构。

## M1 API 契约层

**目标**：把 Apple 官方 OpenAPI 规范变成可控的开发期类型边界。

**范围**：固定官方规范的获取来源、选择生成方式、建立可重复的再生成流程、约定生成产物的存放与评审方式。

**设计要点**：生成产物是边界工件，只约束资源、参数和响应形态，不承载业务逻辑；同一规范输入必须产出一致结果，规范版本与采集时间需要被记录。

**退出标准**：

- 契约可以从官方规范一键再生成，结果可复现。
- [架构总览](../architecture/overview.md) 描述的契约升级三步（更新契约、检查兼容、回归测试）具备可执行基础。

**风险**：官方规范体量大，生成产物的可读性与评审策略需要在本阶段定调，避免后续每次升级都依赖人工逐行比对。

## M2 认证与请求核心

**目标**：交付所有能力共用的认证与请求地基。

**范围**：按 [认证实现策略](../implementation/authentication.md) 实现凭证签发、复用与保守刷新；按 [请求模型](../implementation/request-model.md) 实现薄请求层——认证注入、查询表达、错误归一、限流信息透传。

**退出标准**：

- 凭据缺失、凭证失效、权限不足、限流四类场景可被区分，并有对应的错误语义。
- 并发触发刷新时复用同一次签发结果。
- 集成测试覆盖认证与请求构造，fixture 不含可用的真实凭据。

**风险**：签名细节与 Apple 实际行为的偏差只能靠真实环境暴露，需要安排一次真实凭据冒烟验证；凭据经环境变量注入，不进入仓库。

## M3 分页与首批读能力

**目标**：用真实读取场景验证地基，交付第一条端到端链路。

**范围**：分页模块（游标续读、单页/限量/全量语义、失败页上下文保留）；以应用列表、应用详情和版本信息读取作为验证切片。

**退出标准**：

- 业务调用不接触游标即可读取多页数据。
- 应用与版本读取能力在真实账号下冒烟通过。
- 权限不足、资源不存在等错误路径的反馈符合错误分类策略。

## M4 元数据与评论能力、最小 Skill 入口

**目标**：把读能力扩展为读写能力面，并第一次以 Skill 形态暴露。

**范围**：版本元数据与本地化的读取和更新；评分与评论读取、开发者回复；覆盖既有能力的最小 Skill 入口与任务组织雏形。

**设计要点**：Skill 入口此时只需证明 agent 能以任务语义调用能力并得到可读反馈，能力清单不求全；输出必须区分"Apple API 不支持"与"本项目暂未实现"，这是 [产品范围](../product/api-scope.md) 的硬性设计要求。

**退出标准**：

- 元数据与本地化更新链路在测试账号验证通过，评论读取与回复可用。
- 通过 agent 实际调用 Skill 完成一次真实任务，例如读取应用元数据并修改一处文案。

## M5 报表工作流

**目标**：交付第一个多步骤工作流，验证工作流层设计。

**范围**：销售/财务报表与 Analytics 报表的任务创建、状态推进、文件下载与解析边界；权限不足、报表未生成、文件损坏等可诊断错误。

**退出标准**：

- 报表从发起请求到本地结构化结果全链路可用。
- 轮询与状态推进细节不泄漏给业务调用方。
- 个人 key 与团队 key 的权限差异有明确反馈。

## M6 媒体素材工作流

**目标**：交付截图与预览素材上传链路。

**范围**：上传上下文创建、按 ASC 返回的上传计划推进传输、完整性校验与提交确认；失败时保留素材、阶段与 ASC 资源状态。

**退出标准**：

- 单张截图与整组素材上传在测试应用验证通过。
- 流程中断后可以诊断出停在哪个阶段。

## M7 TestFlight 与送审准备

**目标**：补齐产品首期范围的最后两块能力，形成能力闭环。

**范围**：TestFlight 基础管理（测试组、测试员、构建分发可见性、反馈读取）；送审准备与发布配置（送审材料检查、版本提交相关资源）。build 二进制上传明确不在范围内，TestFlight 能力围绕已存在的构建展开。

**退出标准**：

- [产品范围](../product/api-scope.md) 首期列出的六项能力全部可经 Skill 调用。
- 每项能力有任务级反馈与错误提示。

## M8 Skill 完备化与发布

**目标**：把"能力可用"打磨成"可分发的 Skill"。

**范围**：任务组织与描述打磨、输入校验、用户可读反馈的统一口径、使用文档（配置与凭据说明）、分发打包（单文件 CLI 产物与 plugin/marketplace 形式，见 [Skill 入口与 CLI 策略](../implementation/skill-interface.md)的运行环境保障）。

**分发形态**：对外按官方 plugin 布局打包（清单 + `skills/` + 自带单文件 CLI，由 `${CLAUDE_PLUGIN_ROOT}` 寻址），经 marketplace 分发，与既有 sesamehut marketplace 约定一致。分发采用单仓库形态：payload 提交于本仓 `plugin/` 子目录，marketplace 用 `git-subdir` 源指向它（不另立 plugin 仓库，详见 [Skill 入口与 CLI 策略](../implementation/skill-interface.md)的分发路径）。开发态项目级 skill 与分发态 plugin skill 并存，SKILL.md 内容保持单一来源以防漂移。官方无安装钩子，故"零安装步骤"完全依赖单文件 bundle。

**退出标准**：

- 新用户按文档可以从零完成配置并跑通一个真实任务。
- Skill 描述准确反映能力边界，agent 不会被误导去调用不存在的能力。
- 分发包安装后无需执行任何依赖安装步骤即可运行，环境前提仅为 Node。
- 经 marketplace 安装的 plugin 与开发态项目级 skill 行为一致。

## M9+ 扩展与持续事项

**方向**：对标 `App-Store-Connect-CLI` 的能力广度并追平，超越点在受众——让不写代码的用户用自然语言驱动全流程。M9+ 不收缩功能面，按价值把缺口逐块补齐。

不设固定排期，按价值拉动；每项仍以“一条端到端可验证链路”收尾。落地任一项前，先按 [架构总览](../architecture/overview.md) 的契约升级策略确认对应资源已在再生成的契约中，再复核 [产品范围](../product/api-scope.md) 的边界判断。下列项按“离现有能力的距离”从近到远排列。

### 域内写能力补全（最近，扩展已交付的域）

Apple API 支持、当前只读或部分写、设计决策中暂缓的写（属“暂未实现”而非“Apple 不支持”）。这些写还没登记进 `registry`，调用会落到未知子命令；补全时应一并登记到 planned 路径：

- **分阶段发布写控制**——当前只读 `appStoreVersionPhasedRelease` 状态，缺启用、暂停、恢复、加速到全量的写（设计决策 B2 暂缓）。
- **年龄分级声明写入**——当前只读 `ageRatingDeclaration`，缺问卷 PATCH（设计决策 D1 暂缓）。
- **出口合规声明文档**——当前仅写 Build 上的 `usesNonExemptEncryption` 布尔位，缺 `appEncryptionDeclaration` 文档上传这一独立的 upload-like 流程（设计决策 E2 暂缓）。

### 扩展能力（新开资源域）

[产品范围](../product/api-scope.md) 已列为扩展能力，按商业价值拉动：

- **商业化资源**——内购、订阅与订阅组、价格与价格点、促销优惠/优惠码/挽回优惠、地区可用性；内购测试所需的 sandbox 测试员一并评估。
- **营销资源**——自定义产品页、应用内事件、产品页实验（实验结果分析按 [产品范围](../product/api-scope.md) 仍暂缓）。

### 受限能力（API 支持但受角色/账号约束）

- **组织与访问**——公开 API 支持范围内的用户、角色与邀请管理（创建/下载 API key 仍按 [产品范围](../product/api-scope.md) 明确不支持）。
- **Provisioning**——证书、Bundle IDs、设备、配置文件等签名材料；本地代码签名/打包不在此列（见下文“明确划在范围外”）。

### 后续集成（依赖额外资格或独立子系统）

- **Xcode Cloud**——产品、工作流与构建运行等 CI 资源。
- **Webhooks**——ASC webhooks 订阅与事件配置（自建接收端仍按 [产品范围](../product/api-scope.md) 暂缓）。
- **EU 替代分发**——替代分发密钥、marketplace 等条件能力，依赖账号资格与业务条款。
- **其他较新资源域**——App Clips 体验、Game Center、App Store 提名（nominations）等。这些随 Apple spec 演进，实现前必须对照再生成的契约确认资源形态，避免按过期研究动工。

### 持续事项（维护与可信度）

- **契约升级**——Apple 规范升级按 [架构总览](../architecture/overview.md) 的升级策略执行；周更的契约漂移检查已在监控新版本发布。
- **媒体端点迁移预案**——截图/预览上传当前骑在 Apple `@deprecated` 端点上，是唯一会被 Apple 单方面打断的硬依赖；持续跟踪官方新上传模型，提前准备迁移路径（见 M6 [阶段计划](archive/m6-media-workflows.md) 的弃用端点隔离策略）。
- **行为实机核实**——消化代码中标注的 live-verify 项（如裸建测试员是否发邀请邮件、`usesNonExemptEncryption=false` 是否单独清合规、release 的异步与不可逆语义），用冒烟脚本的可逆/只读路径逐项验证并回写文档，把“暂标待核实”升级为“经验证”。
- **生态复核**——当官方 SDK、官方 MCP server 或社区事实标准出现时，按 [SDK 对比](../research/sdk-comparison.md) 与 [生态调研](../research/mcp-skill-landscape.md) 列出的复核点重新评估。

### 对标并超越的延伸方向（跨出当前 ASC-only 边界）

要追平 `App-Store-Connect-CLI` 的广度，下列方向超出当前“纯 ASC REST API 薄层”的架构。它们不被排除，但纳入前需要先在 [架构总览](../architecture/overview.md) 与 [产品范围](../product/api-scope.md) 中确认架构与信任边界的扩张，再立项：

- **Apple Search Ads / Apple Ads**——独立的 Apple Search Ads Campaign Management API；要覆盖广告能力需新增一条认证与契约线，与现有 ASC 契约并列。
- **本地 Apple 工具链编排**——Xcode 构建、公证（notarization）、代码签名、build 二进制上传等本地能力。对 agent 而言这是“在用户机器上代为执行命令”，对不写代码的用户价值很大，但会把信任边界从“只调 Apple REST API”扩展到“执行本地命令”，需专门设计与显式确认。
- **第三方服务集成（如 RevenueCat、ASO 平台）**——非 Apple 官方 API，作为可选增值能力评估。

Apple 根本没有公开 API 的能力（协议/税务/银行/收款、审核中心往来消息、创建/下载 API key）属 Apple 的硬限制——竞品同样无法自动化，这不是本项目的取舍。这类只能在网页端完成的动作，本 skill 的职责是用自然语言把前置条件和操作步骤讲清楚，引导用户去网页端完成，而不是假装能做。
